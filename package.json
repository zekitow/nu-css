{
  "name": "visual-identity-definition-model",
  "version": "0.0.1-alpha",
  "description": "Base template about the concepts that you have to know before starting a new visual identity for a project.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "José Ribeiro Filho",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-sass": "^3.1.0"
  }
}
